namespace WorkForceManager.Domain.Enums;

/// <summary>Trạng thái đơn đăng ký làm thêm giờ (OT): chờ duyệt → duyệt/từ chối.</summary>
public enum OvertimeStatus
{
    Pending,
    Approved,
    Rejected
}
